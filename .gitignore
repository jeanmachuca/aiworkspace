# Ignore all subdirectories (each is its own independent git repo)
/*/

# But keep the scaffolding directories
!/.github/
!/.opencode/

# Dependencies
node_modules/

# Session files
sessions/
.sessions/
package.json
package-lock.json